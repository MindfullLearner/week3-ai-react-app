// Favourites View
// Will later render the Favourites page user interface.

const FavouritesView = () => {
  return (
    <div>
      {/* Favourites page content will go here */}
    </div>
  );
};

export default FavouritesView;
