// Auth View
// Will later render the authentication (login/register) user interface.

const AuthView = () => {
  return (
    <div>
      {/* Auth page content will go here */}
    </div>
  );
};

export default AuthView;